module adder (
	// three 1-bit inputs
    input a[1], 
    input b[1],
    input c[1],

    // 2-bit output
    output out[2]
  ) {

  always {
    // add all inputs together
    out = a + b + c;
  }
}
